<UserControl x:Class="ComicReader.Views.ReadingCoreControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="400">
    <Border Background="#111" CornerRadius="6" Padding="8" Width="360">
        <StackPanel>
            <TextBlock Text="Modo Lectura" Foreground="White" FontWeight="Bold" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <RadioButton x:Name="RbSingle" Content="Una página" GroupName="view" Margin="0,0,8,0" Checked="ViewMode_Checked" Tag="SinglePage"/>
                <RadioButton x:Name="RbContinuous" Content="Scroll" GroupName="view" Margin="0,0,8,0" Checked="ViewMode_Checked" Tag="ContinuousScroll"/>
            </StackPanel>
            <TextBlock Text="Separación entre páginas" Foreground="#ccc" FontSize="12"/>
            <Slider x:Name="SpacingSlider" Minimum="0" Maximum="64" ValueChanged="SpacingSlider_ValueChanged"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Cerrar" Click="Close_Click" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
