<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ComicReader.Views.FavoritesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ComicReader.Converters"
        Title="Favoritos y Colecciones" 
        Width="1200" Height="800"
        MinWidth="1000" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.Resources>
        <!-- Convertidores -->
        <local:PathToFilenameConverter x:Key="PathToFilenameConverter"/>
        
        <!-- Estilos modernos usando recursos dinámicos de temas -->
        <Style x:Key="ModernTabStyle" TargetType="TabItem">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1,1,1,0" CornerRadius="8,8,0,0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" 
                                              ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HeaderBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource HeaderTextColorBrush}"/>
                                <Setter Property="Panel.ZIndex" Value="100"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.97" ScaleY="0.97"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" BasedOn="{StaticResource ModernButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonPrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonPrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonPrimaryBorderBrush}"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" BasedOn="{StaticResource ModernButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonSecondaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonSecondaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonSecondaryBorderBrush}"/>
        </Style>

        <Style x:Key="DangerButtonStyle" BasedOn="{StaticResource ModernButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonDangerBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonDangerTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonDangerBorderBrush}"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Direction="315" ShadowDepth="4" Opacity="0.1" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource FocusBorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FavoriteItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="{DynamicResource ItemBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ItemSelectedBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header con gradiente dinámico -->
        <Border Grid.Row="0" Background="{DynamicResource HeaderBackgroundBrush}" Padding="30,25">
            <StackPanel>
                <TextBlock Text="⭐ FAVORITOS Y COLECCIONES" 
                           FontSize="32" FontWeight="Black" 
                           Foreground="{DynamicResource HeaderTextColorBrush}" 
                           HorizontalAlignment="Center"
                           FontFamily="{DynamicResource Font.Header}"/>
                <TextBlock Text="Organiza tus cómics favoritos y crea colecciones personalizadas temáticas" 
                           FontSize="16" Foreground="{DynamicResource HeaderTextColorBrush}" 
                           HorizontalAlignment="Center" Margin="0,8,0,0" Opacity="0.9"/>
            </StackPanel>
        </Border>

        <!-- Contenido Principal con Pestañas -->
        <TabControl Grid.Row="1" Margin="20" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabControl">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabControl">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                                    <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" 
                                            CornerRadius="0,12,12,12" Padding="0">
                                        <ContentPresenter ContentSource="SelectedContent"/>
                                    </Border>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Pestaña de Favoritos -->
            <TabItem Header="💖 MIS FAVORITOS" Style="{StaticResource ModernTabStyle}">
                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Título y Acciones Favoritos -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Cómics Favoritos Individuales" 
                                   FontSize="22" FontWeight="Bold" 
                                   Foreground="{DynamicResource PrimaryBrush}" 
                                   VerticalAlignment="Center"/>
                        <Button Style="{StaticResource PrimaryButtonStyle}"
                                Click="AddFavorite_Click" Margin="30,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💖" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Agregar Favorito"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource DangerButtonStyle}"
                                Click="RemoveFavorite_Click" Margin="10,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💔" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Quitar Favorito"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SecondaryButtonStyle}"
                                Click="RefreshThumbnails_Click" Margin="10,0,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Refrescar Portadas"/>
                            </StackPanel>
                        </Button>
                        <TextBlock Text="Total: " Margin="30,0,4,0" VerticalAlignment="Center" 
                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryBrush}" 
                                   FontWeight="SemiBold" Text="{Binding IndividualFavorites.Count}"/>
                    </StackPanel>

                    <!-- Búsqueda de Favoritos -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="🔍" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Name="SearchFavoritesTextBox" Width="350" Style="{StaticResource SearchBoxStyle}"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 TextChanged="SearchFavorites_TextChanged">
                            <TextBox.Resources>
                                <Style TargetType="TextBox" BasedOn="{StaticResource SearchBoxStyle}">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="Buscar en favoritos..." Foreground="{DynamicResource TextDisabledBrush}" Margin="12,0,0,0"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <ComboBox Name="FavoritesSortComboBox" Width="180" Margin="15,0,0,0"
                                  Background="{DynamicResource CardBackgroundBrush}"
                                  Foreground="{DynamicResource TextBrush}"
                                  BorderBrush="{DynamicResource BorderBrush}"
                                  SelectionChanged="FavoritesSort_SelectionChanged">
                            <ComboBoxItem Content="Fecha agregado (reciente)"/>
                            <ComboBoxItem Content="Fecha agregado (antiguo)"/>
                            <ComboBoxItem Content="Nombre (A-Z)"/>
                            <ComboBoxItem Content="Nombre (Z-A)"/>
                            <ComboBoxItem Content="Última lectura"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Lista de Favoritos -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <ListBox Name="FavoritesListBox" Background="Transparent" BorderThickness="0"
                                 SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ItemsSource="{Binding IndividualFavorites}">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource FavoriteItemStyle}"/>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="180" Height="320" CornerRadius="12" 
                                            Background="{DynamicResource CardBackgroundBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                            Margin="8">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.2"/>
                                        </Border.Effect>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="200"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            
                                            <!-- Miniatura -->
                                            <Border Grid.Row="0" CornerRadius="8,8,0,0" ClipToBounds="True" Margin="4,4,4,0">
                                                <Grid>
                                                    <!-- Fondo placeholder -->
                                                    <Rectangle Fill="{DynamicResource DisabledBrush}"/>
                                                    
                                                    <!-- Imagen real -->
                                                    <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" 
                                                           StretchDirection="Both"/>
                                                    
                                                    <!-- Placeholder visual cuando no hay thumbnail -->
                                                    <Grid>
                                                        <Grid.Style>
                                                            <Style TargetType="Grid">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Thumbnail}" Value="">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Grid.Style>
                                                        <Rectangle Fill="{DynamicResource DisabledBrush}" Opacity="0.3"/>
                                                        <TextBlock Text="📚" FontSize="48" 
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource TextBrush}" Opacity="0.7"/>
                                                        <TextBlock Text="Cargando portada..." FontSize="10" 
                                                                   HorizontalAlignment="Center" VerticalAlignment="Bottom"
                                                                   Foreground="{DynamicResource TextBrush}" 
                                                                   Margin="0,0,0,20" Opacity="0.7"/>
                                                    </Grid>
                                                    <Border Background="#80000000" CornerRadius="0"
                                                            Visibility="Collapsed">
                                                        <TextBlock Text="NUEVO" Foreground="White" FontWeight="Bold"
                                                                   HorizontalAlignment="Right" VerticalAlignment="Top"
                                                                   Margin="8" FontSize="10"/>
                                                    </Border>
                                                    <!-- Botón eliminar en la esquina -->
                                                    <Button HorizontalAlignment="Right" VerticalAlignment="Top" 
                                                            Margin="8" Width="24" Height="24"
                                                            Background="#80FF4444" BorderThickness="0"
                                                            Click="RemoveFromFavorites_Click">
                                                        <TextBlock Text="✕" Foreground="White" FontWeight="Bold" FontSize="12"/>
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}"
                                                                                    CornerRadius="12" BorderThickness="0">
                                                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                                                  VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#FFFF4444"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                            
                                            <!-- Información -->
                                            <StackPanel Grid.Row="1" Margin="12,8,12,4">
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="13"
                                                           Foreground="{DynamicResource TextBrush}"
                                                           TextTrimming="CharacterEllipsis" TextWrapping="Wrap"
                                                           MaxHeight="34"/>
                                                <TextBlock Text="{Binding FilePath, Converter={StaticResource PathToFilenameConverter}}" 
                                                           FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"
                                                           TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"
                                                           Margin="0,2,0,0"/>
                                                <TextBlock Text="{Binding DateAdded, StringFormat='Agregado: {0:dd/MM/yy}'}" 
                                                           FontSize="9" Foreground="{DynamicResource TextDisabledBrush}"
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>
                                            
                                            <!-- Botones de acción -->
                                            <StackPanel Grid.Row="2" Orientation="Horizontal" 
                                                        HorizontalAlignment="Center" Margin="8">
                                                <Button Content="📖 Leer" FontSize="10" Padding="8,4"
                                                        Click="OpenComic_Click" Style="{StaticResource PrimaryButtonStyle}"
                                                        Margin="0,0,4,0"/>
                                                <Button Content="📁" FontSize="10" Padding="6,4"
                                                        Click="ShowInExplorer_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Pestaña de Colecciones -->
            <TabItem Header="📚 COLECCIONES" Style="{StaticResource ModernTabStyle}">
                <Grid Margin="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="320"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Panel Izquierdo - Lista de Colecciones -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="📚 MIS COLECCIONES" FontSize="18" FontWeight="Bold" 
                                       Foreground="{DynamicResource PrimaryBrush}" Margin="0,0,0,20"/>
                            
                            <!-- Botones de Acción -->
                            <Button Style="{StaticResource PrimaryButtonStyle}"
                                    Click="CreateCollection_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Nueva Colección"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Click="RenameCollection_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="✏️" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Renombrar"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Click="DuplicateCollection_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📄" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Duplicar"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource DangerButtonStyle}"
                                    Click="DeleteCollection_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🗑️" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="Eliminar"/>
                                </StackPanel>
                            </Button>

                            <!-- Lista de Colecciones -->
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0" MaxHeight="400">
                                <ListBox Name="CollectionsListBox" BorderThickness="0" Background="Transparent"
                                         SelectionChanged="CollectionsListBox_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource ItemBackgroundBrush}" 
                                                    CornerRadius="12" Padding="16" Margin="0,4"
                                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource ItemSelectedBrush}"/>
                                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Border.ContextMenu>
                                                    <ContextMenu>
                                                        <MenuItem Header="✏️ Renombrar" Click="RenameCollection_Click"/>
                                                        <MenuItem Header="📄 Duplicar" Click="DuplicateCollection_Click"/>
                                                        <MenuItem Header="🎨 Cambiar color" Click="ChangeCollectionColor_Click"/>
                                                        <Separator/>
                                                        <MenuItem Header="🗑️ Eliminar" Click="DeleteCollection_Click"/>
                                                    </ContextMenu>
                                                </Border.ContextMenu>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Width="18" Height="18" CornerRadius="9" Margin="0,4,12,0"
                                                            Background="{Binding Color}"/>
                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="15"
                                                                   Foreground="{DynamicResource TextBrush}"/>
                                                        <TextBlock Text="{Binding ItemCount, StringFormat='{}{0} cómics'}" 
                                                                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                                                        <TextBlock Text="{Binding Description}" 
                                                                   Foreground="{DynamicResource TextDisabledBrush}" FontSize="11" 
                                                                   TextTrimming="CharacterEllipsis" Margin="0,2,0,0"
                                                                   MaxHeight="32" TextWrapping="Wrap"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>

                    <!-- Panel Derecho - Contenido de Colección -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="8,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Header de Colección -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                                <TextBlock Name="CollectionTitleText" Text="Selecciona una colección" 
                                           FontSize="22" FontWeight="Bold" 
                                           Foreground="{DynamicResource PrimaryBrush}" 
                                           VerticalAlignment="Center"/>
                                <Button Style="{StaticResource PrimaryButtonStyle}"
                                        Click="AddToCollection_Click" Margin="30,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📖" FontSize="14" Margin="0,0,5,0"/>
                                        <TextBlock Text="Agregar Cómic"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource SecondaryButtonStyle}"
                                        Click="AddFolderToCollection_Click" Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📂" FontSize="14" Margin="0,0,5,0"/>
                                        <TextBlock Text="Agregar Carpeta"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource DangerButtonStyle}"
                                        Click="RemoveSelected_Click" Margin="10,0,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🗑️" FontSize="14" Margin="0,0,5,0"/>
                                        <TextBlock Text="Quitar"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <!-- Filtros y Búsqueda de Colección -->
                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBox Name="SearchCollectionTextBox" Width="300" Style="{StaticResource SearchBoxStyle}"
                                         TextChanged="SearchCollection_TextChanged">
                                    <TextBox.Resources>
                                        <Style TargetType="TextBox" BasedOn="{StaticResource SearchBoxStyle}">
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                                <VisualBrush.Visual>
                                                                    <TextBlock Text="Buscar en colección..." Foreground="{DynamicResource TextDisabledBrush}" Margin="12,0,0,0"/>
                                                                </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Resources>
                                </TextBox>
                                <ComboBox Name="CollectionSortComboBox" Width="150" Margin="15,0,0,0"
                                          Background="{DynamicResource CardBackgroundBrush}"
                                          Foreground="{DynamicResource TextBrush}"
                                          BorderBrush="{DynamicResource BorderBrush}">
                                    <ComboBoxItem Content="Nombre A-Z"/>
                                    <ComboBoxItem Content="Nombre Z-A"/>
                                    <ComboBoxItem Content="Fecha agregado"/>
                                    <ComboBoxItem Content="Última lectura"/>
                                </ComboBox>
                                <TextBlock Text="Total: " Margin="20,0,4,0" VerticalAlignment="Center" 
                                           Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock VerticalAlignment="Center" Foreground="{DynamicResource PrimaryBrush}" 
                                           FontWeight="SemiBold" Text="{Binding ElementName=CollectionItemsListBox, Path=Items.Count}"/>
                            </StackPanel>

                            <!-- Contenido de la Colección -->
                            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                <ListBox Name="CollectionItemsListBox" Background="Transparent" BorderThickness="0"
                                         SelectionMode="Extended" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                    <ListBox.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ListBox.ItemsPanel>
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource FavoriteItemStyle}"/>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Width="160" Height="240" CornerRadius="12" 
                                                    Background="{DynamicResource CardBackgroundBrush}" 
                                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="*"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    
                                                    <!-- Miniatura -->
                                                    <Border Grid.Row="0" CornerRadius="8" Margin="6,6,6,3">
                                                        <Grid>
                                                            <Rectangle Fill="{DynamicResource PlaceholderImageBrush}"/>
                                                            <Image Source="{Binding Thumbnail}" Stretch="UniformToFill"/>
                                                        </Grid>
                                                    </Border>
                                                    
                                                    <!-- Información -->
                                                    <StackPanel Grid.Row="1" Margin="6,3,6,6">
                                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="11"
                                                                   Foreground="{DynamicResource TextBrush}"
                                                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                                        <TextBlock Text="{Binding Path}" FontSize="9"
                                                                   Foreground="{DynamicResource TextDisabledBrush}"
                                                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{DynamicResource FooterBackgroundBrush}" Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Style="{StaticResource PrimaryButtonStyle}" Click="Export_Click" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📤" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock Text="Exportar"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SecondaryButtonStyle}" Click="Import_Click" Margin="5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📥" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock Text="Importar"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource DangerButtonStyle}" Click="Close_Click" Margin="15,5,5,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌" FontSize="14" Margin="0,0,5,0"/>
                        <TextBlock Text="Cerrar"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
